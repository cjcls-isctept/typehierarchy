package pt.iscte.pidesco.hierarchy.model;

import java.io.File;

public class ClassElement {

/**
 * Represents a class element
 * @isMain represents if said instance is the one represented in the type hierarchy view
 * 
 */
	public ClassElement(ClassElement parent, File file, boolean isMain) {
		if(parent == null) {
			//quer dizer que é root ?
		}
			
	}
	
}